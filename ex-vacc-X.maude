***(
 Analysis with I-S
***)

load ex-vaccines3 .

mod TEST is
    pr VACCINES .

    --- --------------------
    var STATE      : State .
    var SETEDGE SE : SetEdge .
    var AG         : Agent .
    --- --------------------

    --- eq model = x-s .
    eq moduleName = 'TEST .
    eq update(STATE, SETEDGE, AG) = update-x-s(STATE, SETEDGE, AG) .
    eq strategy(SE, STATE) = i-x(SE, STATE) .
endm

--- search  init =>* STATE such that consensus(STATE) .